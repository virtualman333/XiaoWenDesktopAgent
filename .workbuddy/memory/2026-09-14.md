# 2026-09-14 工作日志

## v1.0.4：语音识别改造 + 修复对话 HTTP 400

### 1. 语音识别方案替换（核心）

**问题**：语音输入完全不可用，永远报 `network` 错误。
**根因**：原实现用 Chromium 内置 `webkitSpeechRecognition`，该能力由 Google 云端提供，
国内网络必然不可达。属于技术选型错误，不是网络抖动。
**方案**：改用阿里云百炼 paraformer-realtime（WebSocket 实时流式识别）。用户已提供
DashScope API Key 并写入 `config.json` 的 `asrApiKey`。

**实现**：
- 主进程新增 `asr:start` / `asr:audio` / `asr:stop` / `asr:cancel` / `asr:test`
- 渲染进程 `speech.js` 重写：`getUserMedia` + AudioWorklet（降级 ScriptProcessor）
  采集 16kHz 单声道 PCM，重采样转 Int16，按 100ms 分片发给主进程
- 音频以**二进制帧**发送，不做 base64
- 静音超过 `asrSilenceMs`（默认 2s）自动结束并发送
- 设置页新增：识别方式选择、百炼 API Key、模型名、静音时长、测试按钮
- 系统内置识别保留为 `provider='system'` 备选

### 2. ⚠️ 关键坑：Electron 主进程没有 WebSocket

**这是本次最容易踩、且我已经踩过的坑**：

| 环境 | Node 版本 | 全局 WebSocket |
|---|---|---|
| 系统 Node 22 | 22.22 | ✅ 有 |
| Electron 32 内置 | **20.18.1** | ❌ **undefined** |

全局 `WebSocket` 到 Node 22 才默认可用。用系统 Node 做协议验证会通过，
但**在 Electron 主进程里必然失败**。必须 `npm i ws`。

另外 `ws` 与浏览器实现的**回调签名不同**：
- `ws`：`onmessage = (data, isBinary)`，data 直接是内容
- 浏览器：`onmessage = (event)`，内容在 `event.data`

已写兼容层 `readTextFrame()` 统一处理。发送音频时显式转 Buffer +
`{ binary: true }`，确保走二进制帧。

### 3. 修复对话 HTTP 400

**错误信息**：`ai is not one of ['system','assistant','user','tool','function']`
**根因**：界面内部一直用 `'ai'` 作角色名（气泡 CSS 类名、历史记录），
发请求时没转成 `'assistant'`。
**修复**：
- `panel.js` 新增 `normalizeRole()`，渲染层构造 payload 时归一化
- `main.js` 发请求前再做一层归一化兜底（防老历史数据）
- `messages` 数组统一存 `'assistant'`，UI 类名仍用 `'ai'`
- 清洗了 config.json 历史里 1 条 `'ai'` 记录

**验证**：role=ai → HTTP 400（与用户报错一致），role=assistant → HTTP 200。

### 4. 环境坑（持续有效）

- **bash 缺 coreutils**：`ls` / `wc` / `dirname` / `tail` 全部 not found。
  一律用 Python 执行 shell 命令。
- **`ELECTRON_RUN_AS_NODE=1` 泄漏**：当前工具环境设了这个变量，导致用
  `electron.exe` 启动应用时 `require('electron')` 返回**字符串**（exe 路径），
  `app` 为 undefined → 启动即崩。启动 Electron 前必须从 env 里删掉它。
- **`app.getPath('userData')` 在独立脚本里是 `AppData\Roaming\Electron`**，
  不是 `xiaowen-assistant`。诊断脚本要显式 `app.setPath('userData', ...)`。
- **tasklist 输出是 GBK**，Python 读要 `decode('gbk')`，否则 UnicodeDecodeError。
- **Bash 调用结束会杀掉整个进程树**，所以无法用「后台启动 + 后续连接 CDP」
  的方式验证 Electron。改为**一次性脚本**：启动→验证→输出→exit，同一进程内跑完。
- **诊断脚本要加 `--disable-gpu --no-sandbox`**，否则 GPU 进程反复崩溃会连带
  终止主进程（FATAL: GPU process isn't usable. Goodbye.）。
- 沙箱会拦截目录删除（safe-delete），打包目录被占用时改输出到新目录。

### 5. 交付

- 提交 `0c36f4a`，标签 `v1.0.4`，已推送 main + tag
- 安装包：`release-v1.0.4b/小问助手 Setup 1.0.4.exe`（76.4 MB）
  （`release-v1.0.4` 目录因文件占用未能清理，产物实际在 `4b`）
- ASR 全链路验证：run-task → 30 帧/96000 字节 → finish-task → task-finished

---

## v1.1.0「贾维斯版」：Agent / MCP / Skills / 记忆人格 / 语音三方案 / 开机自启

用户需求：优化界面体验、开机自启、Skills+MCP、bash/浏览器/整机操作能力、
短期+长期记忆、人格（知道我是谁主人是谁）、语音输出多方案自选。

### 架构：新增 `src/main/jarvis/` 模块（8 个文件）

| 文件 | 职责 |
|---|---|
| store.js | persona/memory/sessions/mcp/tools 统一存储（userData/jarvis/） |
| agent.js | Agent 循环：流式 + tool_calls 累积，最多 8 轮，高危确认（IPC 双向握手） |
| tools.js | 17 个内置工具（shell/文件/进程/截屏/剪贴板/通知/HTTP/记忆…） |
| mcp.js | MCP 客户端：stdio（按行 JSON-RPC）+ http（Streamable，SSE data: 解析） |
| skills.js | SKILL.md frontmatter 解析，清单注入 + load_skill 按需加载 |
| tts.js | 三方案：web / dashscope CosyVoice / openai 兼容 |
| autostart.js | Win 注册表 Run / macOS LoginItem / Linux XDG |
| index.js | 全部 IPC 注册 + MCP 自动重连 |

### 实测协议要点（CosyVoice TTS，花钱验证过）

- 与 ASR 共用 `wss://dashscope.aliyuncs.com/api-ws/v1/inference`
- `run-task`(task_group=audio, task=tts, function=SpeechSynthesizer, 文本放 input.text)
  → `task-started` → N×(`result-generated` JSON + 紧随的**二进制音频帧**)
- **JSON 控制帧也是二进制下发的**：按首字节 `0x7b`(`{`) 区分，否则会把元数据当音频
- 服务端**不主动发 task-finished**：用「静默 900ms 无新帧」判定结束
- mp3（ID3 头）与 wav（RIFF）都实测有效；v2 音色带 `_v2` 后缀
- 实测可用音色 v2：longxiaochun/xiaoxia/cheng/fei/shuo/tian/yuan/jielidou_v2
  （wanqing/yuting/momo/bujie/xiang/xin_v2 会 task-failed）

### 安全设计

- `DANGEROUS_PATTERNS` 正则拦截 format/shutdown/reg/diskpart/dd 等
- `assertWritable()`：写/删仅限 home、下载、tmp、userData、白名单（EPATH 错误）
- 确认模式三档：danger（默认）/ all / none；确认经 `agent:confirm` → 渲染层弹窗 → `agent:confirm-reply`

### 踩坑记录（本次新增）

1. **并行 Edit 同一文件会丢改动**：package.json 两次并行 Edit，第二次（output 目录）被吞，
   导致 electron-builder 仍输出到旧目录 win-unpacked 被占用而失败。同一文件必须串行编辑。
2. index.js 曾引用未定义的 `abortAgent`（同样源于并行编辑丢行）—— UI smoke 抓住的。
3. `ELECTRON_RUN_AS_NODE` 必须在 **spawn 的 env 里删**（脚本里 `delete process.env` 无效，
   Electron 在 node_init 阶段、用户代码执行前就读取了）。正确姿势：node 父进程 spawnSync
   传干净 env；bash 里 `ELECTRON_RUN_AS_NODE=` 置空也无效（存在即真）。
4. headless（无窗口）Electron 里 `clipboard.readText()` 返回空：读不到系统剪贴板，
   属环境限制，真实 GUI 正常。已加 PowerShell Get-Clipboard 兜底。
5. MCP stdio 在 Windows：command 为 `npx`/`node` 等无扩展名时 spawn 解析不到
  （PATHEXT 问题），用 `shell: true` 让 cmd.exe 解析最稳。
6. npm 在沙箱被策略拦截（黑名单 wsl.exe），改用
  `node node_modules/vite/bin/vite.js build` + `node node_modules/electron-builder/cli.js`。

### 验证

- `build/_jarvis_e2e.js`：22/22 通过（人格/记忆/PS+cmd/高危拦截/文件读写删/越权拦截/
  进程列表/剪贴板/截屏 7MB/CosyVoice 67KB MP3/自启状态/Skills/工具定义/MCP 三连）
- `build/_fake_mcp_server.js` + `McpManager` 联调：connect/tools-list/tools-call 全通
- `build/_ui_smoke.js`：对话页 + 设置页 8 分组零报错，截图确认（人格已生效：
  问候语显示「永贵，随时吩咐」）

### 交付

- 提交 `d9e98de`，标签 `v1.1.0`，已推送 main + tag
- 安装包：`release-v1.1.0/小问助手 Setup 1.1.0.exe`（76.5 MB）
- 产物/验证脚本保留在 build/ 供回归：_jarvis_e2e.js、_fake_mcp_server.js、_ui_smoke.js
