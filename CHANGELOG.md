# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.13.0] - 2026-09-18

### 为什么值得做

「定时任务」有一个并发池（默认同时跑 2 个，超出的排队）。主进程把池子里的情况算得清清楚楚 ——
`schedule.status()` 会返回排队几个、并发上限几个、接下来 5 次几点跑、最近 20 条运行记录 ——
preload 也早就把它暴露成 `xw.scheduleStatus()`：

```
scheduleStatus: () => ipcRenderer.invoke('schedule:status'),
```

**而渲染层一次都没调用过它。** 面板只拿 `scheduleList()`，于是这些数字只活在内存里：
点了「跑一次」没反应的人，无从知道任务只是排在了队里；排了「每 30 分钟」的任务，
也看不到下几次到底几点。

顺着这条线往下查，队列本身还有三处会咬人：

| 问题 | 后果 |
|---|---|
| 「正在跑」那道闸只看 `runningSince`，管不到「排在队里、一次都还没跑」的任务 | 并发满时连点两下「跑一次」，队列里躺两条 → 前一个跑完把它接着再跑一遍，结论也再播报一次。而 README 明写着「同一任务重复触发只跑一份」 |
| `remove()` / `update()` 只动 `schedules.json`，不碰队列 | 删掉（或暂停）的任务如果正排着队，轮到时照样会跑、照样主动播报 ——「我删了它，它还是来敲我」 |
| 面板自己维护的 `schRunning` 只在收到 start/done 事件时更新 | 面板一重载它就是空的：正在跑的任务被显示成**空闲**，「跑一次」按钮重新可点 |

另有一处「说了能改、其实改不了」：README 写着 `schedConcurrency` **可调 1–4**，
而全仓只有 `schedule.js` 的 `maxConcurrency()` 读它 —— 没有界面、不在 `DEFAULT_CONFIG`，
连默认值都只能从代码里那句 `: 2` 反推。

### 怎么变

**设置页「定时任务」顶部新增「调度概览」**，四样此前只在内存里的数据第一次上界面：

| 显示什么 | 数据来源 |
|---|---|
| 启用任务 / 运行中 / 排队 / 并发上限 / 调度开关 | `status()` 的 active / total / running / queued / concurrency / enabled |
| 接下来 5 次分别几点跑 | `status().next`（已按 `nextAt` 升序） |
| 最近 20 条运行记录 | `status().log`（失败标红、排过队的注明「（排过队）」） |

单个任务那一行挂上「排队中」标签、按钮变「已排队」；并发满时点「跑一次」不再是
「点了没反应」，而是明确告诉你「前面还有任务在跑，已排队」。

队列侧的三处一并修掉：

- **去重**：`runTask()` 在入队前先查队列里有没有同一个任务，有就直接返回「已经在队列里了」
  （不再往里塞第二条）；临时任务（`runOnce`，不落库）用 `transient` 标出来，不能按「不存在」处理
- **出队**：`remove()` 与 `update({enabled:false})` 顺手把该任务还没轮到的排队项摘掉；
  `pump()` 自己**再兜一道** —— 开跑前重新查一遍任务还在不在（别的会话直接改了
  `schedules.json` 时也要拦得住），拦下来就发一条 `drop` 事件、记一条日志
- **退出作废**：`stop()` 清空队列 —— 留着只会在下次 `start()` 后被 pump 跑出来，
  那是一个「隔了一次重启才冒出来」的旧结果

界面侧：面板重载后以主进程的 `running` / `runningInfo` 重建本地那份「谁在跑」
（`elapsedMs` 换算回开始时刻，任务行上的秒数接着往上走）；为此把纯映射抽成
`schRunningFromStatus()`，可在 Node 里直接喂假数据验。

并发上限也补上了控件（设置 → 定时任务，1–4），`schedConcurrency` 进了 `DEFAULT_CONFIG`。
界面**不自己夹值**：回显走主进程夹取后的 `status().concurrency`，值不在选项里就不动下拉 ——
避免「界面显示 4、实际跑 2」这种不一致。

### 新增 `npm run test:schedule`（104 项，已接入 `test:all`）

| 组 | 查什么 |
|---|---|
| 真跑调度逻辑 | 用假 store + 可控闸门的假 agent 顶掉 I/O 与模型，**调度本身是真的**：真加任务、真并发限流、真排队、真 pump、真落运行历史。含空状态六字段、`next` 升序与 5 条上限、并发上限的夹取与回落、排队与自动放行、失败也进历史、落盘 60 / 展示 20 两个上限不是一回事 |
| 渲染层读没读 | `schRunningFromStatus()` 抽出来当场喂假数据跑（含 `null` / 缺字段 / 只有 runningInfo 三态）；接线层读源码确认「调了、用到了、落到 DOM 上」—— 这一半是弱判据，测试里写明了 |
| 队列的三条规矩 | 重复触发只排一份（只跑一次、只播报一次）、排队期间被删除 / 暂停立刻出队且不跑、pump 兜底（外部改文件也要拦住）、`stop()` 作废、临时任务不被误丢 |

反向验证：**26 处**单缺陷注入（含把本轮修复逐条改回去），逐条只注入一个缺陷、
核对红的是哪条断言，源码按 sha256 逐字节还原。

---

## [1.12.0] - 2026-09-18

### 为什么值得做

小问能**执行命令、写文件、删文件**。README 对这两件事写了两句承诺：

- 高危命令（format / shutdown / reg / diskpart 等）**直接拦截**
- 写 / 删操作**只允许**用户目录、下载、临时目录与白名单目录

而这两句底下**一条断言都没有**。判据写死在 `tools.js` 里，那个文件第一行就 `require('electron')` ——
纯 Node 加载不了它。`test:all` 里 18 套测试、上千条断言，没有任何一条碰过它
（`tools.js` 只被 `_jarvis_e2e.js` 这个需要真 Electron 的一次性脚本 require 过）。
安全边界全靠「读代码看着没问题」撑着。

把它拆出来单测之后，立刻发现了三个真问题。

### 怎么变

**判据搬进 `src/main/jarvis/tool-guard.js`**（纯函数、零 electron 依赖）；
`tools.js` 只负责把 `app.getPath()` 给的几个根目录喂进去 —— 那是它唯一没法抽走的东西。

顺带修掉三处（每一处都有对应断言钉着）：

| 洞 | 后果 | 修法 |
|---|---|---|
| **目录联接 / 软链接绕过** | Downloads 里放一个指向 `C:\Windows` 的 junction，字面路径落在允许目录内 → 判定放行，写下去进了系统目录 | 判定前先对「已存在的最深祖先」做 realpath，再把还不存在的那几段接回去（`file_write` 的目标通常是新文件，不能直接对整条路径 realpath） |
| **`rd /s /q` 与 `rmdir /s` 完全不拦** | 与 `del /s` 同级的递归删除，名单里一条都没有 | 补 `rd` / `rmdir` 规则 |
| **`del /q /s` 绕开 `format.com c:` 绕开** | 旧写法只认紧跟 `del` 的那一个斜杠参数；`format.com` 因为多了扩展名而绕开 `format\s+[a-z]:` | 补 `del /q /s` 与 `format.com` 规则 |

另外把 `startsWith(r + path.sep) || startsWith(r + '/')` 换成 `path.relative` ——
原来那是同一件事写了两遍（Windows 上后一半是死代码），而 `path.win32.relative`
内部就按不区分大小写比较，正合 Windows 的语义。

### 新增 `npm run test:tools`（33 项，已接入 `test:all`）

| 组 | 查什么 |
|---|---|
| 高危命令 | 23 条正例必须全被拦下；14 条常用良性命令一条都不许误报；**名单里不许有哑弹**（每条规则都至少有一个正例命中 —— 永远不响的规则比没有规则更坏，它让人以为防线在工作） |
| 写路径围栏 | 边界相等 / 子树 / 前缀混淆（`<root>-evil`）/ `..` 穿越 / Windows 大小写 / **真的建一个目录联接验绕过** / `roots` 里的空值不许变成放行 / `EPATH` 错误码 / 返回解析后的绝对路径 |
| 注册表 ⇄ README | 工具名唯一、每个都有 `description` 与 `parameters`；README 的「N 个内置工具」必须等于注册表长度；每个工具都要在 README 里有文档 |

两组反向对照写进了测试本身：把名单清空后正例必须**全部**漏网（证明拦截确实由名单承担，
不是别的什么在兜），以及未建联接时同一目录下的普通路径照常放行（证明不是「一律拒绝」）。

### 顺带修掉一处已存在的文档漂移

README 的 Agent 能力表写着「**17** 个内置工具」，而同一个 README 的文件树注释写着「**19** 个」——
表格里整整漏了 `meeting_record` / `meeting_minutes` 两个会议工具（v1.8 加的，没回到那张表）。
现在总数改为 19、表格补上会议一行，并且**这一致性由测试兜着**：下次再加工具忘了回填文档，`test:all` 会红。

---

## [1.11.0] - 2026-09-17

### 为什么值得做

上一版的剪贴板感知有个断点：内容被带进输入框时是**裸文本**。点开通知看到一段报错，还得自己打一句「帮我看看这段报错，什么原因」才能按回车 —— 而「复制这段」这个动作本身就已经表达了意图。问法该由小问补上。

### 怎么变

| 内容类型 | 带进输入框的内容 |
|---|---|
| 报错 | 「帮我看看这段报错，是什么原因、怎么改？」+ 围栏包住的原文 |
| 代码 | 「帮我看看这段代码，有没有问题？」+ 围栏包住的原文 |
| JSON | 「帮我看看这段 JSON 有没有问题？」+ 围栏包住的原文 |
| 链接 | 「帮我看看这个链接讲的是什么，有值得注意的吗？」+ 链接（不加围栏，留着自己点） |
| 长文本 | 「帮我看看这段内容，提炼下要点。」+ 原文 |

问句**可以直接回车，也可以改** —— 仍然不代发，发不发由你决定（这条边界没变）。

围栏长度会按内容里最长的连续反引号自动加长：粘来的代码自己带 ``` 时，等长围栏会被提前闭合，后面的内容会跑到代码块外面去。

录制一个意图模板只需维护 `clip-sense.js` 的 `ASK_TEMPLATES` 一处，问法在主进程生成后随 IPC 一起送给面板，渲染进程不复制一份（两份写法必然漂移）。

### 自测

`test:clip` 97 → 148 项断言。新增覆盖：五类内容各自带意图说明且**原文一字不改**、含 ``` 的内容不能被自己的围栏截断、空 / null / 纯空白不出问法、认不出的类型退回原样 + 中性提示。

### 识别规则可配置

内置的 5 类（报错 / 代码 / JSON / 链接 / 长文本）覆盖的是通用开发者场景。但每个人还有自己的世界：公司内部日志前缀 `[corp]`、内部域名、自家框架特有的栈帧格式……内置规则一律认不出来，于是这个功能对这类人等于半残 —— 而这些完全不该由我们猜。

配置写在 `config.json` 的 `clipSenseRules`，**改完立即生效，不必重启**：

```json
{
  "clipSenseEnabled": true,
  "clipSenseRules": {
    "disableKinds": ["url"],
    "ignorePatterns": ["^\\[内部\\]"],
    "customKinds": [
      {
        "id": "corp-log",
        "label": "公司日志",
        "pattern": "^\\[corp\\]",
        "flags": "im",
        "title": "这段公司日志要我看看吗？",
        "ask": "帮我分析这段公司日志。"
      }
    ]
  }
}
```

| 字段 | 作用 |
|---|---|
| `disableKinds` | 关掉不想被提示的内置类型（只能填 `error` / `json` / `url` / `code` / `longtext`） |
| `ignorePatterns` | 永不提示：命中即静默，**压过自定义类型** |
| `customKinds[].pattern` | 你自己写的正则；`flags` 默认空，`i` / `m` 常用 |
| `customKinds[].ask` / `title` | 这一类的问法与通知标题；不填按 `label` 生成兜底说法 |

判定顺序：**凭证 → ignorePatterns → customKinds → 内置 5 类**。「别打扰我」比「认出来」更强，所以自己写的黑名单压过自己写的白名单；自己配的类型优先于我们的猜测。

**一条不可配置的边界：凭证识别永远是第一步，且没有开关。** 不提供 `disableKinds: ["secret"]` 这种写法 —— 写了只会得到一条「未知类型」警告并被忽略。理由与上一版相同：误报一次，密钥就印在系统通知横幅上了，收不回来。这条捷径不该开放。同理，`customKinds` 的 `id` 也不允许占用 `secret` / `ignore` 或内置类型名。

正则写岔了（编译不过、超长、flags 有非法字符、缺 `id`、`id` 重复）只会记一条日志警告并跳过那一条，**不会影响其它规则，也不会让轮询停摆**；`g` / `y` 标志会被自动去掉（带 `g` 的正则 `test()` 有 `lastIndex` 状态，会让同一段内容时提示时不提示）。

### 自测（规则可配置）

`test:clip` 148 → 240 项断言。除功能覆盖外，专门钉死了几条容易悄悄退化的性质：凭证判定先于 `ignorePatterns` 与 `customKinds`、`disableKinds: ["secret"]` 被拒绝、`g` 标志被去掉（连续三次判定结果必须一致）、各类坏配置只警告不抛异常、规则对象自身抛异常时轮询器仍能继续工作。

### 规则现在能在设置界面里改

规则可配置之后，改一条规则的实际步骤是：找到 `%APPDATA%\xiaowen-assistant\config.json` → 用编辑器打开 → 在 JSON 里找准位置 → 存盘。而且**写错一个字母不会有任何反馈**：把 `ignorePatterns` 少写一个 `s`，规则一条都不生效，只在日志里留一行警告 —— 用户只会觉得「我明明填了」。连开关本身都只存在于配置文件里，界面上一个字都没有。

设置页新增「📋 剪贴板感知」：

| 部分 | 说明 |
|---|---|
| 开关 | 这项功能默认关闭（常驻读剪贴板属隐私敏感行为），此前界面上没有任何入口 |
| 规则编辑框 | 与 `config.json` 的 `clipSenseRules` 是同一份，改完保存立即生效，不必重启 |
| 生效结果 | 实时显示**实际编译出的结果**：仍然提示哪些类型、关掉了哪些、忽略正则几条、自定义类型几个 |
| 警告 | 规则里被丢掉的条目不静默 —— 逐条列出，并**点名到你写错的那个字段** |
| 插入示例 / 清空 | 一份可直接用的模板；清空 = 只用内置 5 类 |

两条关键设计：

- **显示「生效结果」而不是原文**。`compileRules` 对写错的条目一律只警告不抛，所以「填了 5 条、生效 1 条」完全可能；只回显原文的话，用户永远看不出哪几条被丢了。摘要里的「仍然提示：…」就是回答这个问题的。
- **未知字段点名报警**（在 `compileRules` 里做的，所以手改配置文件同样受益）：

  ```
  clipSenseRules 里有未知字段「ignorePattern」，已忽略（只认：disableKinds / ignorePatterns / customKinds）
  ```

  校验不过（JSON 语法错误、顶层不是对象）**不允许保存**；能保存的必然是校验通过的那一份。

校验走主进程（`clip-sense.js` 的 `parseRulesText`），界面不重复实现一遍规则知识 —— 否则「界面说没问题、实际全被丢弃」这种漂移迟早会发生。摘要里还固定写着「凭证始终识别（不可关闭）」：改「关闭哪些类型」时最容易产生的误解，就是「那我能不能把密钥那条也关了」。

### 自测（设置界面校验）

`test:clip` 240 → 291 项断言，新增一节专测规则文本校验：空文本 = 只用内置 5 类（不是错误）、7 种语法与结构错误全部拦下且给出说明、未知字段点名报警且摘要如实显示「0 条生效」、`disableKinds: ["secret"]` 被拒绝但不算致命错误、一条坏正则不影响另一条好正则生效、`id` 冲突与缺字段各给一条警告。断言里不用 `r.summary.custom[0].label` 这种写法（被测对象坏掉时会先抛 TypeError，把「哪条锁断了」盖成「脚本崩了」），统一走安全取值 + 兜底值。

### 规则对不对，不用再猜：设置里能「拿一段内容试试」

上一版给了规则编辑框，但**验证规则只有一条路**：保存 → 去复制一段内容 → 看桌宠会不会动。而且失败时的表现是「什么都不发生」—— 主人无法区分下面这几种情况：

| 你以为是 | 实际可能是 |
|---|---|
| 规则写错了 | 写法没问题，只是**前面有条 `ignorePatterns` 先命中了** |
| 正则有问题 | 正则没问题，只是**命中的那一类被 `disableKinds` 关掉了** |
| 功能坏了 | 内容**太短**（门槛 12 字符），本来就不该打扰你 |
| 类型选错了 | 认出来了，但**是被凭证保护按下去的**（这条不可绕过） |

`compileRules` 只对**写坏**的规则报警（正则编译不过、字段名不认识），对「规则本身没毛病、但在这台机器上永远不会命中」一声不吭 —— 而后者恰恰是最常见的那种。判定链（凭证 → 忽略正则 → 自定义类型 → 内置 5 类）是**先到先得**的硬编码顺序，从界面上完全看不出来是「写错了」还是「压根没轮到它」。

设置页新增「拿一段内容试试」：

| 元素 | 说明 |
|---|---|
| 输入框 | 贴一段内容，边打边给结论（250ms 防抖）。**改规则后不必先保存就能试** |
| 用剪贴板里的内容 | 一键把当前剪贴板拿进来试 —— 就是真出问题那一刻的那段内容 |
| 会提示 | 判成了哪一类（自定义类型会显示你自己起的名字）+ 横幅上会显示什么 + **会自动填进输入框的完整问法** |
| 不会提示 | 直接说明**为什么**：太短 / 疑似凭证 / 命中哪条忽略正则 / 哪一类被你关了 / 认不出 |

三处刻意的做法：

- **判定链从注释变成了数据**。`classifyClipboard` 原先对「不提示」只有一种表达（`worth: false`），五种原因挤在一起；现在返回里带 `reason`（`empty` / `too-short` / `secret` / `ignored` / `disabled` / `unrecognized` / `hit`）、`stage`（判定停在链上哪一环）和 `detail`（命中的是**哪条正则**、**哪一类**）。
- **试跑看到的就是真结论**。问法由主进程的 `buildClipQuestion` 生成 —— 与真提示走的是同一个函数，不是另写一份演示文案；另写一份的话，界面说「会填成这样」而实际填的不是，比不显示更糟。
- **内置类型的显示名只剩一份**。此前 `panel.js` 自己抄了一张 `CLIP_KIND_LABELS`，主进程新增一类而界面漏改就会静默显示原始 id（`longtext`），且没有任何东西会报错。现在由主进程随校验结果下发，并用断言钉住「`BUILTIN_LABELS` 的键必须与 `BUILTIN_KINDS` 一一对应」。

试跑是**只读**的：不读真剪贴板、不落盘、不改状态，随时可以试。

### 自测（试跑）

`test:clip` 291 → **356** 项断言。新增两节：判定理由（五种「不提示」的原因各自分得开、`detail` 指向真正命中的那条规则）与试跑（`verdict` 带显示名/阶段/人话理由/完整问法、未保存的规则也能试、规则文本坏掉时短路并原样带出 JSON 错误、连试两次结论一致且不污染不传规则时的默认行为）。另加四组跨文件一致性锁：`panel.js` 不得再出现第二份内置类型清单、`main.js` 的 `clip:test` 必须复用 `clipSense.testRules`、`preload.js` 必须指向 `clip:test`、`REASON_TEXT` 必须覆盖全部理由取值。

**11 处缺陷注入逐条验证锁是否真的会红**（含两条反方向），全部恰好命中预期的那几条断言，源码逐字节还原：其中一条把 `classifyClipboard` 的判定循环改回「关掉就跳过」的老写法 —— 结果只有新增的「理由」类断言变红，老断言一条未动。这是「这次重构没改判定行为、只多了理由」的直接证据。

### 剪贴板规则可以导出 / 导入了

**为什么值得做**

规则写在一台机器上，换台机器就得手抄一遍 JSON —— 规则越长越容易抄错，而抄错的表现是「有的规则莫名不生效」，正是最难查的一类。而且没有导出就没有留档：想对比「调参前 / 调参后」两套规则，只能靠记忆。

**怎么变**

设置 → 📋 剪贴板感知 的规则编辑框下面多了两个按钮：

- **导出到文件**：把**输入框里当前这一份**（含还没保存的编辑）写成一个 `.json`。导出前先过一遍校验 —— 一份自己都编译不过的规则导出去没有意义，原因是哪条正则写错了，最该知道的是导出方。
- **从文件导入**：选一个 JSON，读进输入框并立刻显示生效摘要。**不自动保存** —— 换机器时最怕「导进来一份不对的规则直接生效」，那样连原来的规则都没了。看过摘要、确认无误，再点「保存规则」。

默认文件名带日期（`clip-sense-rules-20260916.json`），便于留档与多版本对比；导入有 256 KB 上限，选错文件（比如选了个安装包）会直说「文件太大」而不是把整个文件读进内存。

三处刻意的做法：

- **导入不碰配置**。写 `config.json` 的路径始终只有用户点「保存规则」这一条；导入只把**文本**拿回来，合法与否仍走同一个 `clip:rules-parse`。少一条写配置的路径，就少一处「界面上没点保存却生效了」。
- **导出复用校验，不另写一份**。`rulesExportText` 内部就是 `parseRulesText`：另写一份的话，界面上的摘要说「这条正则被丢掉了」而导出的文件里还留着它，两边各说各话。
- **内置类型的清单只剩一份，且是派生的**。`BUILTIN_KINDS` 此前是与 `BUILTIN_LABELS` 并列的手写数组，加一类要改两处；漏改的后果是静默的（界面显示原始 id，或者这个类型关不掉），而且两处都不报错。现在由 `Object.keys(BUILTIN_LABELS)` 派生，并用断言钉住「不许退回手写数组」—— 只断言「两张表一一对应」是不够的：照抄一份完全相同的数组也能通过，漂移从下一次新增类型才开始。

**自测**

`test:clip` 356 → **393** 项断言。新增三节：导出（合法规则可导出、导出的文本能被原样读回且语义不变、语法错 / 顶层是数组时拒绝导出且不给文本、空规则标记 `empty` 并附一句人话提醒、写坏的条目其警告要一起带出来）、默认文件名（月/日补零、非法入参与 `Invalid Date` 都退回当天而不是拼出 `NaN`）、文件大小上限。另加一组接线锁：导出/导入的主进程处理器、`preload`、`panel.js` 调用、`panel.html` 按钮**四处缺一处就静默失效**（少写 preload 是运行时报错，少写按钮是点都点不到、没有任何提示），以及「导入块里不许出现 `setConfig` / `saveClipRules`」这条行为锁。

**8 处缺陷注入逐条验证锁是否真的会红**，全部恰好命中预期断言，源码逐字节还原：`BUILTIN_KINDS` 退回手写数组（含「照抄一份完全相同的数组」这条最容易漏的）、手写数组多一类但忘配显示名、导出不校验、导出文本不再以换行结尾、文件名不补零且月份少加一、导入不限制大小、面板上少一个按钮、导入后自动保存。

### 会议自动检测：修好一个「一关就永久失效」的开关

**为什么值得做**

设置页里「摄像头被占用也算在开会」这个开关，是给你**少采一路信号**用的。但代码里麦克风的注册表探针写成了同一个条件：

```
c.meetingWatchCam === false ? Promise.resolve('') : readConsent('microphone')
```

于是取消勾选它，**麦克风信号也一起没了**。麦克风是判定「在开会」的主判据，没了它 `classify` 最高只能给出「有应用在用麦」，永远不会判成 `meeting` —— 自动检测与自动记录**整体静默失效**：不报错、不提示、设置页显示一切正常，只是再也不会有任何会议被记下来。默认值是「开」，所以只有主动关掉这一路的人才会踩到，而关掉之后没有任何其他表现。

**怎么变**

把「这一轮该看哪几路信号」抽成一个函数，只判断一次：

| 调用 | 结果 |
|---|---|
| `watchedDevices({})` | `['microphone', 'webcam']` |
| `watchedDevices({ meetingWatchCam: false })` | `['microphone']` |

`sampleOnce` 只按结果取数据，自己不再看配置；并用断言钉死「`sampleOnce` 里不许再出现 `meetingWatchCam`」—— 两路探针串同一个开关，就是这次出问题的写法。

这里要跟另一个开关分清楚：**麦克风信号没有开关**。设置页的「同时采集麦克风」(`meetingCaptureMic`) 管的是**录制**时带不带自己的声音，跟检测无关；检测侧的麦克风信号始终都在采。

**顺手让这件事不再无声**

- 设置页「检测我现在是不是在开会」现在会先说**这一轮探了哪几路信号**（`本轮探测：麦克风 + 摄像头` / `…（摄像头信号按设置关闭）`）。没采到的信号不再只是「没有」——「环境安静」和「这一路压根没采」本来就是两回事。
- 「麦克风占用」为空时明写「没有程序在用」，而不是把这一行整个省掉。
- 「摄像头被占用也算在开会」下面补了一句「只影响这一路信号；麦克风信号始终都采」。

**自测**

`test:meeting` 177 → **196** 项断言。新增一节专测探针归属：`watchedDevices` 在三种配置下的结果、**关掉摄像头开关后麦克风占用照样读得到**（端到端走 `sampleOnce`，并断言真的读 / 没读对应的注册表）、反向对照（开着开关时纯视频会议仍判成在开会）、以及两条形态锁。

**5 处缺陷注入逐条验证锁是否真的会红**，全部恰好命中预期断言，源码逐字节还原 —— 第一处就是把上面那个笔误原样写回去，新增的断言里有 2 条应声变红；另加一条**反向对照**：把麦克风那行改成等价的 `watchedDevices(c).includes('microphone') ? … : …`，必须仍然全绿（证明确实没把断言写成「认字面量」）。

### 导出的规则文件带上了格式版本（补记）

`3fc5b73` 落地时漏了更新日志，这里补上。

导出的规则 JSON 此前没有任何结构版本号，导入时只能靠「有没有这个字段」反推格式。将来给规则加字段（比如 `customKinds` 增加 `weight`），旧版本程序导入新文件只会打印一句「未知字段」然后**照样收下** —— 用户以为迁移成功了，其实丢了一部分。

- 导出文件的第一个键是 `$schema`，值形如 `xwda-clip-rules/1`；版本号来自代码常量，不散落字面量。
- **没有标记的文件视为第 1 版照常接受**（手写与旧版本导出的都长这样），并如实报告「这份文件没有格式标记」。
- **更高版本的文件明确拒绝**：静默收下等于偷偷丢字段。错误里说清是版本问题，并给出「升级程序」这个动作。
- **认不出的 `$schema` 直接拒绝**：VS Code 的 `settings.json` 第一行也长这样，以前选错文件只会得到一串「未知字段」，看上去像规则写错了，其实文件压根不对。
- 导入时先剥掉 `$schema`，不写进 `config.json`，否则下一次导出会再叠一层。

自测 `test:clip` 393 → 418 项断言。

---

## [1.10.0] - 2026-09-16

这一版让小问**会留意你复制的东西** —— 复制一段报错、一段代码、一个链接之后，它自己先开口问一句「这段要我看看吗」。

### 为什么值得做

以前的流程是死的：先发现有东西要问 → 按快捷键 → 粘贴 → 再打字。可「复制」这个动作本身已经表达了意图 —— 复制一段报错的人，十有八九是准备拿去问人或搜的。既然小问就常驻在桌面上，这一步该由它来接。

### 怎么工作

| 环节 | 行为 |
|---|---|
| 认出 | 每 1.5 秒看一眼剪贴板，分类成报错 / 代码 / JSON / 链接 / 长文本 |
| 提示 | 宠物动一下 + 托盘气泡 + 一条可点击的系统通知（静音，不打断） |
| 点它 | 打开对话面板，内容已经在输入框里等着 —— 只填入，不代发 |
| 不打扰 | 同一段内容只问一次；提示后 20 秒冷却，连续复制不会被刷屏 |

### 三条边界

1. **默认关闭**。常驻读剪贴板属隐私敏感行为，和语音唤醒同一个理由：开不开由你决定。
2. **疑似凭证绝不提示**。API Key、token、私钥、JWT、`password = ...` 一律静默跳过 —— 提示就等于把密钥印在通知横幅上，那比不提示更糟。
3. **不联网、不代发**。分类全是本地正则，内容只在内存里过一遍、不落盘；填进输入框后发不发仍由你按 Enter。

### 其它

- 新增 `test:clip`（97 项断言）并接入 `test:all`：凭证识别逐个样例钉死，去重 / 冷却 / 读失败容错都有断言
- 测试在开发过程中抓到一个真 bug：冷却期的初始值用 `0` 占位，会让**启动后头 20 秒的提示被全部吞掉** —— 已改用 `null` 表示「还没提示过」

---

## [1.9.0] - 2026-09-14

这一版让**桌面宠物真正顶替悬浮球** —— 开着宠物，悬浮球就自动退场，桌面上只留一个入口。

### 为什么值得做

宠物和悬浮球其实是**同一个入口的两种样子**：都能单击聊天、双击语音问答、拖着换位置、
右键出菜单，还能常驻监听唤醒词。功能完全重叠，同时摆在桌面上只会互相抢地盘，
用户也不知道该点哪个。以前更别扭的是：把宠物开着，两只东西就一起飘在右下角。

现在的规则是「同一时刻只出现一个」：

| 宠物 | 悬浮球档位 | 桌面上是谁 |
|---|---|---|
| 开着 | 自动（默认） | **宠物**（完全代替悬浮球） |
| 开着 | 总是显示 | 两个都在（想留球当快捷入口的少数派） |
| 开着 | 不显示 | 宠物 |
| 关着 | 任意 | **悬浮球**（一定回来） |

最后一行是底线：**桌面上不能两个入口都没有**。宠物关掉时无论配置怎么写，悬浮球都会回来 ——
以前出现过「两个都关了，桌面上什么都点不到，只能去任务管理器」的情况。

### 宠物接管了悬浮球的全部能力

- **语音唤醒常驻监听**搬到了宠物身上。唤醒引擎还是原来那个（`wake.js`），
  只是换了个宿主：球不在场时由宠物开麦；球在场（用户选了「总是显示」）时还是球听，
  免得两个窗口同时抢麦克风、互相顶掉对方的识别会话。
- **单击 = 摸一把 + 打开对话面板**，**双击 = 语音问答**，和悬浮球的操作完全一致。
  单击要等 260ms 才判定是不是双击，正好把「摸头」的反馈塞进这个空档，手不会觉得空着。
- 右键菜单补齐：聊天 / 语音问答 / 设置 / 置顶 / 悬浮球开关 / 换成悬浮球 / 退出。
  悬浮球不在场时，宠物这个菜单就是唯一顺手的退出入口。
- 主进程的短提示（「对话历史已清空」这类）原来只发给悬浮球，现在两边都发。
- 宠物头顶多了一只「耳朵」小标：唤醒监听开着就亮，「在听…」时会呼吸 ——
  麦克风一直开着这件事得让人看得见。

### 界面

- 设置 →「通用 → 悬浮球」新增档位选择（自动 / 总是显示 / 不显示），
  下面一行实时说明**现在桌面上到底是谁**、为什么是它。
- 「桌面宠物」页的说明改写：讲清开着宠物时悬浮球会隐藏、宠物能做什么。
- 宠物体检多了「桌面入口」一项；Ctrl+K 多了一条「悬浮球 / 宠物 换个班」。
- 托盘菜单和悬浮球右键菜单都能一键换人。

### 一处容易写漂的地方，用测试钉住了

主进程是 CommonJS、渲染层是 ES Module，「谁上场」这套规则没法共用一个文件，
只能各写一份。所以把它抽成了两个纯函数模块（`src/main/entry.js` 与
`src/renderer/js/entry-rule.js`），并用同一张真值表喂给两边，
断言 **12 种组合逐格一致** —— 谁改漏了一边，测试立刻红。

### 测试流程简化（响应「流程耽误迭代速度」）

- **去掉 GUI 冒烟测试**：界面冒烟、启动冒烟、宠物页冒烟、主动关注冒烟、宠物主进程冒烟
  全部删除。它们要在真实图形会话里跑、动辄十几秒、还容易 flake，
  在快速迭代阶段性价比太低。
- CI 里那个 `continue-on-error` 的「GUI 冒烟」任务整段删除，只剩纯 Node 回归。
- `npm test` 现在就是**全量白盒单测**，端到端 **12 秒**跑完
  （以前只有 `test:all` 叫这个名字，还得另外记一串脚本名）。
- 保留 `npm run test:pet-top`（宠物置顶的白盒回归，手动跑）—— 它盯的是
  「宠物被别的置顶窗口盖住」那个反复出过三次的 bug，不在默认流程里。
- 顺手清掉 `build/` 下一批没人引用的死脚本（`_pet_smoke.js`、`_proactive_smoke.js`、
  `_pet_main_test.js`、遗留的 `_commit_msg.tmp`）。

### 新增测试

- `npm run test:entry`（53 项，纯 Node）：档位归一化、六种组合的完整真值表、
  「两个入口都消失」的底线、唤醒宿主判定、状态概要、主进程与渲染层规则逐格比对、
  以及从旧版本升级上来（配置里没有 `ballEnabled`）的兼容行为。

全量：entry 53 / context 65 / hotkey 43 / meeting 177 / proactive 105 / orch 59 /
pet 32 / tray 19 / wake 19，共 572 项，全绿。

---

## [1.8.0] - 2026-09-14

这一版让小问会**自己发现你在开会**，然后一边听一边把内容记成带要点和待办的纪要。

### 新增：会议 / 视频 / 语音通话自动检测

Windows 没有「现在在开会吗」这种接口，所以靠三个客观信号凑出来：

- **谁占着麦克风 / 摄像头** —— Windows 把这件事写在注册表里
  （`CapabilityAccessManager\ConsentStore\microphone|webcam`），
  `LastUsedTimeStop = 0` 就表示还在用。这是最硬的信号。
- **进程列表** —— `tasklist /v` 拿进程名 + 窗口标题，用来把「谁在用麦」翻译成人话，
  也用来识别浏览器里的网页会议。
- **窗口标题** —— 命中「语音通话 / 正在共享 / Google Meet」这类关键词。

光看标题会误报：腾讯会议、钉钉、飞书都爱开机常驻、窗口一直开着，
所以**标题必须配合「麦克风真的被占用」才算数**。另外还有两道过滤：

- **小问自己用麦克风不算** —— 语音问答也会占麦，不排除掉就会「一问就以为在开会」。
- **进程已退出的注册表残留不算** —— 系统偶尔会留下没人认领的记录。

判定用**迟滞**而不是瞬时：连续采到 N 次（默认 2 次、每 5 秒一次）才算「开始」，
连续 M 秒（默认 90 秒）采不到才算「结束」。宁可晚几秒开始，也别一开机就误报。

### 新增：自动记录 + 自动生成纪要

- **采集的是系统声音（Windows loopback）+ 麦克风**，所以对方说话也算数 ——
  不需要装虚拟声卡。两路音频在 Web Audio 里直接求和成一路 16kHz 单声道 PCM。
- 走的是已有的语音识别通道（Key 和 WebSocket 都在主进程），采集页只是一个隐藏窗口。
- **长会议会自己换连接**：每 N 分钟（默认 5）主动轮换一条识别会话，
  换的期间音频先排队、新会话起来立刻补发，不丢话。
- 转写会**清洗**：丢掉「嗯嗯嗯」这类噪声、丢掉反复吐出来的重复句
  （识别服务在长句上会给不断增长的前缀，比如「大家好」→「大家好我是小王」，这种要合并而不是重复记）。
- 结束后让模型写成结构化纪要：**主题 / 要点 / 结论 / 待办 / 风险与疑问**，
  存成 Markdown（`userData/minutes/<时间>-<应用>.md`）+ 一份 JSON（含全文转写）。
- **三道自动收尾**：连续 N 分钟（默认 5）没人说话、到达单次上限（默认 120 分钟）、
  进程退出前同步落盘（`will-quit` 里没法 await，所以走同步写盘这条路）。
- 内容太少（太短 + 识别不到几句话）就不留纪要，免得攒一堆垃圾。

### 新增：默认「先告诉你，再开始录」

检测到会议后会先播报一句（宠物气泡 + 系统通知），再开始记录 —— **绝不偷偷录**。
可以改成「开始前先问我一句」，也可以整体关掉自动记录只留提醒。

- 设置页新增「🎙 会议纪要」分区：采样间隔、判定灵敏度、静默收尾时长、单次上限、
  是否录麦克风、摄像头算不算信号、非白名单应用算不算信号，全都能调。
- **「现在是什么状态」+「立即检测一次」**：把这一轮的判定结论和理由直接摊在界面上 ——
  「为什么不触发」这种问题不用再翻日志。
- 列表里能直接打开 / 删除历史纪要，也能一键打开纪要文件夹。
- 输入框上方会有一条**正在记录**的实时条（显示时长、字数、最近一句），点一下就能收尾出纪要。
- 命令面板（Ctrl+K）新增三条：开始记录 / 结束并出纪要 / 检测我现在是不是在开会。
- 小问也能自己用：新增 `meeting_record`（开始 / 结束 / 查状态）和
  `meeting_minutes`（按关键词找历史纪要、取全文）两个工具。

### 实机验证时挖出来的两个坑

判定逻辑的输入是**系统命令的输出文本**，桩数据再像也不等于真机格式，所以真的在这台机器上跑了一遍
（`npm run probe:meeting`：真读注册表、真列进程，把结论和理由打出来）。结果抓到两个只在真机上才暴露的问题：

- **中文窗口标题全是乱码，中文关键词永远匹配不上。** `tasklist` / `reg query` 按控制台
  OEM 代码页（简体中文是 GBK）输出，而 Node 默认按 UTF-8 解 —— 于是「语音通话」变成「����ͨ��」，
  日志里还看不出任何异常。现在按「能不能被 UTF-8 完整解出来」挑解码方式，
  并且两种解码结果都留着做关键词匹配（判错时另一份就是对的）。
- **中文版 Windows 的 tasklist 把「没有窗口标题」写成「暂缺」，不是 `N/A`。**
  之前会把它当成一个真标题带着走。

顺带确认了注册表的真实结构：应用商店应用是直接用包族名当键名
（`microphone\Microsoft.WindowsCamera_8wekyb3d8bbwe`），不是放在 `Packaged` 子键下；
`#` 就是反斜杠、盘符的冒号保留（`C:#Program Files (x86)#...`）。这些都以真机数据为准写进了判定。

### 测试

- 新增 `npm run test:meeting`（177 项，纯 Node）：注册表解析、tasklist 解析、
  判定（含小问自己占麦、残留记录、常驻托盘、浏览器网页会议、纯视频会议、OBS 直播）、
  迟滞状态机、转写清洗与去重、摘要解析（markdown 与 JSON 两路）、落盘检索，
  以及用桩顶掉 Electron 与子进程后走一遍完整流程（检测 → 自动开录 → 转写 → 摘要 → 落盘 → 播报）。
- `test:boot` 增加两条断言（会议模块必须随启动加载 + 检测必须真的起来）。
- `test:ui` 从 20 项增加到 28 项：会议纪要设置分区、状态回填、switches 回填、
  滑动条标签同步、纪要列表渲染、「立即检测一次」的输出。
- `test:packed` 增加 `dist/minutes.html` 与三个新模块文件，防止采集页漏打包
  （漏了就会「检测到会议但录不了」，而且不报错）。
- 全量：meeting 177 / context 65 / hotkey 43 / proactive 105 / orch 59 / pet 32 /
  tray 19 / wake 19 / pet-top 28 / ui 28 / boot 12。

---

## [1.7.0] - 2026-09-14

这一版是一次「长对话不崩、桌面宠物不再消失、别拿聊天框应付主人」的集中升级。

### 新增：动态上下文 + 自动压缩

长会话到后面会被模型的上下文窗口卡住（要么报错，要么悄悄丢掉开头）。
现在每一轮都按预算动态裁剪，全程不需要主人操心：

- 新增 `src/main/jarvis/context.js`（纯函数，可单独测）：按「中日韩 1 字 ≈ 1 token、
  其它 1/3.5 字符」估算用量，再按 `ctxWindow`（默认 128000）减去回复预留后的余额裁剪。
- 裁剪分四步：**截断超长工具输出** → **最近 N 轮保留原文** → **更早的对话折叠成纪要**
  （`【更早的对话纪要】`，以 system 身份插在保留段之前）→ 还超就整轮丢弃（带保护，不会清空）。
- 工具输出截断保留「头 + 关键行 + 尾」：上千行的构建日志里那条报错常常卡在正中间，
  只留头尾会刚好把它切掉，所以额外把 `error/failed/异常/超时` 这类行捞出来；
  整段是一行超长 JSON 时按关键词定位一小段上下文。
- 会话超过阈值会**异步**让模型写一份滚动摘要存进会话（`store.getSessionSummary`），
  下次压缩直接接上，不会每次都从头开始。
- 设置项：`ctxWindow` / `ctxReplyReserve` / `ctxKeepTurns` / `ctxAutoCompress` / `ctxToolOutputMax`。
- 输入框右下角多了一个**上下文环**：常显当前用量，压缩过就亮「压」角标，
  悬停能看到「压缩前 / 压缩后 / 截断了几条工具输出 / 折叠成几行纪要」。

### 新增：截图快捷键可以改

- 设置 → 截图里，两个快捷键改成**录制式**：点「录制」后直接按键，支持组合键、
  F1–F12、方向键等；旁边有「清空」。录完**立刻试注册**并反馈结果（可用 / 被占用 / 不合法）。
- 修复一个老问题：以前改快捷键只释放了写死的两个默认键，
  **旧键会一直挂在系统里**——于是出现「新键能用、旧键也还在」的怪现象。
  现在统一记录本次注册过的所有键，重注册前先全部撤销。
- 键位被别的程序占用时**如实报错**，不再假装成功；关掉截图开关则一个键都不留。

### 修复：桌面宠物「还是没显示」（第三次，这次找到了真因）

前两次的修复（三重显示、尺寸纠正）都是对的，但没打中要害。这次不靠推理，
而是**直接抽真实合成后的桌面像素**来定位：

> `alwaysOnTop` 只是把窗口放进「最上层那一组」，**组内顺序取决于最后激活时间**。
> 任何后创建的置顶窗口（会议共享画面、全屏播放器、悬浮歌词、下载弹窗）
> 都能把宠物永久盖住 —— 用户看到的就是「宠物没显示」。

修法与加固：

- 宠物改用 `setAlwaysOnTop(true, 'screen-saver')`（比普通置顶高一级，
  实测普通 `alwaysOnTop` 窗口已经盖不住它）；被压住时用 `moveTop()` 重夺最上层。
- 新增 30 秒一次的 `tickTop()` 兜底重夺，配合 `petKeepTop` 开关（关掉就只在召唤/播报时重夺）。
- 自检升级：连续两次判定「不可见」或窗口整个不存在 → **直接销毁重建**（`hardRecover`）。
- 挂钩显示器变化（`display-added` / `display-removed` / `display-metrics-changed`）与
  电源事件（`resume` / `unlock-screen`）：屏幕拓扑或休眠回来造成的「surface 过期」会自动重建。
- 新增 `Alt+Ctrl+P`「召唤宠物到鼠标处」：把宠物挪到鼠标所在那块屏并抢到最上层，
  托盘菜单也有同名项（外加「强制重建宠物窗口」）。
- 设置 → 桌宠新增：置顶开关、防遮挡重夺开关、召唤快捷键录制、三个诊断按钮，
  以及**一段说人话的诊断**（诚实告诉主人「窗口是好的，只是被 XX 盖住了」/「它跑到屏幕外了」）。

### 新增：静默升级 / 静默安装

- 检测到新版本后默认**不再弹对话框打断**：下载完只做轻提示（宠物气泡 + 托盘），
  主人的活照常干。
- `autoUpdateInstallWhenIdle` 打开时，会用 `powerMonitor.getSystemIdleTime()`
  等到主人离开电脑（默认空闲 90 秒）再静默安装并重启 —— 不会在你打字打到一半时重启。
- 想立刻装可以在设置页点「立即静默安装并重启」，或走系统对话框那条老路。
- NSIS 配置补齐 `allowElevation` / `perMachine:false` / `runAfterFinish`，
  让 `/S` 静默安装真正能跑通；`deleteAppDataOnUninstall:false` 保证卸载不丢数据。

### 优化：任务调度改成「先反馈、再给结果」

以前定时任务是同步跑的：主人点了「跑一次」，界面就一直等，跑完才吭声。
现在分两阶段，整个调度器也不再串行：

- **立刻**发一个 start 事件（面板上秒变「运行中」），任务真跑起来；
  **6 秒还没跑完**才补一句「收到，这就去办」——跑得快的任务就一条结论到位，
  不会被「收到」+「结果」两条横幅连着打扰。
- 结论**一定**会投递（回执只代表「我在办」，不能替掉结果）。
- 并发池：默认同时跑 2 个任务（`schedConcurrency`，1–4 可调），超出排队而不是丢任务；
  同一个任务重复触发只会跑一份。
- 超 45 秒补一条「这活儿比平时费点时间，我还在办」。
- 设置页「跑一次」不再阻塞，任务列表实时显示「运行中 Ns」与上次结果；
  新增 `schedule:event` 推送（start / done）。

### 优化：界面不再是一问一答的聊天页

主人明确说过「不希望一问就传统的问答页面」。所以把主界面从**聊天记录**改成了**事情的时间线**：

- **一张卡 = 一件事**：卡头是主人的原话 + 状态徽标 + 序号 + 时间；中间是可折叠的
  **执行轨迹**（调了什么工具、成功还是失败，跑的时候自动展开、跑完自动折叠）；下面是结论；
  卡脚是下一步动作：复制 / 朗读 / 重答 / **⏰ 定成定时任务** / **🧩 派给子代理** / **🧠 记进记忆**。
  一句话的活只有一张卡，回执和结论改写同一张卡，不刷屏。
- **意图预览**：输入框下面常驻一条意图条，边打字边告诉主人「这件事小问会去查资料 /
  动电脑 / 排成定时任务」；命中「长期要做」的（每天 / 提醒我 / 早报）直接把
  **「就这么办」** 按钮递到手边，一键弹出快捷定时对话框。
- **Ctrl+K 命令面板**：模糊搜索并直接执行常用动作（新建工作台、打开设置、
  框选/整屏截图、召唤宠物、新建/查看/立刻跑定时任务、立即看热搜、清空上下文、记剪贴板…），
  找不到命令就把输入的那句话直接交给小问办。
- 主动播报（地震、热搜、定时任务）也是卡，只是换个图标和来源标签，混在同一条时间线上。

### 修复与其它

- 「每天 08:30」「每日9:00」「早上 8:30」「晚上 8 点」「每 2 小时」「每30分钟」
  这类中文写法现在都能被排期解析器认出来（以前只认裸 `08:30`）。
- `hotkeyStatus()` 改为深拷贝返回，设置页的渲染逻辑改不动主进程里的真实状态。
- 会话回放时旧回答不再一直挂着「思考中」徽标，也不再给历史消息挂可点的动作按钮。

### 测试

- 新增 `npm run test:context`（65 项，纯 Node）：词元估算、多模态内容的图片占位、
  工具输出截断（含「报错卡在正中间」这个真实场景）、轮次切分、压缩主流程与统计、
  预算荒唐地小时的保护、滚动摘要的触发条件。
- 新增 `npm run test:hotkey`（43 项，纯 Node + 假快捷键表）：**改键后旧键必须释放**、
  两个键对调、被占用时如实报失败、关掉开关不留幽灵快捷键、试键不留副作用、
  「先试后存」的覆盖层不落盘。
- 新增 `npm run test:pet-top`（28 项，白盒 Electron）：直接在窗口对象上打点，
  断言重夺用的是 `screen-saver` 级别、确实调了 `moveTop()`、
  关掉开关时一个动作都不做、窗口没了不抛异常、硬恢复真的换了新窗口、
  隐藏/显示来回切不会把宠物甩出屏幕。
- 新增 `npm run test:ui`（20 项，Electron）：工作台冒烟——意图条识别、
  快捷定时建卡、Ctrl+K 过滤与 Esc 关闭、**回执与结果改写同一张卡**、
  上下文环的进度与「压」角标、设置页逐标签切换。
- 新增 `npm run test:boot`（10 项，Electron）：真的把主进程起来 9 秒，
  断言托盘 / 宠物 / Jarvis / 截图 / 更新 / 调度全部就位、启动期间无致命日志、
  看门狗第一轮巡检认为宠物「存在 + 可见 + 在屏内 + 置顶」。
  （装配层写错一个名字，单元测试也会全绿，这个能兜住。）
- `test:proactive` 增加「先反馈再给结果」一节（+13 项）：
  跑得快的定时任务只播一条且播的是结论、`kick` 不阻塞、
  同任务不并发两份、并发打满时排队而不是丢任务。
- 新增 `npm run test:all` 串跑全部纯 Node 套件；`build/_run-electron.mjs`
  解决了「开发终端带着 `ELECTRON_RUN_AS_NODE` 导致 electron.exe 被当成 node」的问题。

---

## [1.6.1] - 2026-09-14

### 修复：右下角托盘图标不显示（安装版）

两个坑叠在一起，缺一个都不会出问题：

1. **图标根本没进安装包。** 托盘图标原本只放在仓库的 `build/icon.ico`，
   而 electron-builder 的 `files` 只打包 `src/**`、`dist/**`、`package.json` ——
   `build/` 是 `buildResources`，不进 `app.asar`。于是安装版里
   `path.join(__dirname,'../../build/icon.ico')` 永远不存在，**每次都走兜底**。
   （实测确认：安装版 `app.asar` 里只有 `node_modules / dist / package.json / src`，一个图标文件都没有。）
2. **兜底的内联 base64 是一张损坏的 PNG** —— zlib 流截断、没有 `IEND`，
   `nativeImage.createFromBuffer` 解出来是 `0x0` 的空图。
   而 **`new Tray(空图)` 不会报错**，只是托盘上什么都看不见 —— 纯静默失败。

修法与加固：

- 图标改放 `src/assets/`（`tray.ico` 多尺寸 + `tray.png`），随包发布；
  开发时仍可回退到仓库根的 `build/icon.ico`。
- 图标解析抽成独立的 `src/main/tray-icon.js`，**每一档候选都要过 `isEmpty()` 校验**，
  空图直接跳过换下一档；内联兜底换成一张**完整可解码**的 PNG（同一张图，32×32）。
- `.ico` 直接交给 Windows，由它自己挑 16/24/32… 那一档，比强行 resize 到 16×16 更清楚。
- 托盘创建失败不再拖垮后续初始化（宠物 / Jarvis 照常起来），并写入启动日志。
- 看门狗新增托盘自检：托盘不存在就重建；图标为空则明确报警（附带候选路径），
  设置页「常规」也新增「系统托盘」区块：实时状态 + 「重新载入托盘图标」按钮，
  托盘右键菜单加「重新载入托盘图标」。

### 测试

- 新增 `npm run test:tray`（19 项，纯 Node）：图标资源是否存在、
  `build.files` 是否真的覆盖它、`tray.ico` 各档是否可解码、
  **内联兜底 base64 是否完整且有不透明像素**（正是这次事故的点）。
  （已验证：把 base64 换回旧的那张截断图，测试会失败。）
- 新增 `npm run test:packed`（10 项，纯 Node）：编译出安装目录后，
  直接读 `app.asar` 头部，确认 `src/assets/tray.ico`、`src/main/tray-icon.js`、
  `dist/panel.html` 等运行期资源真的在包里 —— 已接进 Release CI，
  在编译安装包之后、上传产物之前跑，当作最后一道闸。
- 冒烟测试（真 Electron）新增 11 项：图标来源必须是随包的 `src/assets`、
  托盘能建出来、强制走兜底时也不是空图、设置页按钮能走通 preload → IPC → 回填状态。
- 顺带把「体型跑偏能被修正」的用例改强：原来等待 300ms 再读，
  会被散步热路径的纠偏掩盖成恒真；现在改成「立刻读确认改坏」+
  「不喊救援也能被热路径自愈」两个独立断言。
- 打包验证：`release-v1.6.1/win-unpacked` 启动日志确认
  `[tray] 托盘图标就绪 source=…\app.asar\src\assets\tray.ico size=256x256`，
  托盘自检 `iconEmpty:false`、`bounds:[32,48]`。

---

## [1.6.0] - 2026-09-14

### 修复：桌面宠物「不见了」

这次找到的是**真正的根因**（以前修的是表象）。

- **根因**：在这台机器（Windows + 透明窗口 + 软件渲染）上，对宠物窗口调用
  `setPosition()` **每调一次窗口高度就 +1px**。宠物散步时渲染层每秒调十几次，
  窗口于是以约 15px/秒 的速度无限长高 —— 一分钟后已经六七百像素，
  散步逻辑又按「当前高度」重算落脚点，宠物就被一路顶到屏幕上方，
  看起来就是「宠物没显示」。实测 8 分钟内长到 `240x686`，装好的旧版本甚至出现过 `1232x3858`。
- **修法**：移动窗口一律改走 `setBounds()` 并**显式带上宽高**（尺寸绝对指定，不再在
  「当前尺寸」上累加）；高度必须传常量期望值，把量到的尺寸喂回去同样会逐次累加。
  改完后连续散步 2 分钟以上，窗口稳定在 `240x250`（最大 ±1px），再没跑飞。
- **多道保险**（万一将来还有别的路径改尺寸）：
  - 启动 6 秒自检一次，之后**每 60 秒复查**：不可见就唤出，尺寸跑偏就掰回期望值；
    若发现 `setSize` 被系统忽略会记一条日志并停止重试，不和系统来回刷。
  - 散步热路径上一并做廉价纠偏（`get-bounds` / `move-to`），把偏差压到亚秒级。
  - 设置 →「桌面宠物」新增「📍 叫回主屏」按钮 + 实时窗口状态（可见/位置/尺寸），
    托盘菜单也加了「把宠物叫回主屏 / 重新载入宠物」。
- 顺带修掉「宠物开关状态和窗口实际状态可能不一致」：开关统一走一个入口，配置与窗口不会再各说各话。

### 新增：定时任务（你定，小问也能自己定）

- 设置里新增「⏰ 定时任务」页签：新建任务只要一句话 + 一个时间
- 支持 **只跑一次 / 每天 / 每周（可多选星期）/ 每月（几号）/ 每隔 N 分钟**
- 到点由小问**真的跑一轮**（可带工具、MCP、技能、记忆），不是只提醒
- **小问自己也能建任务**：内置 `schedule_task` 工具，你说「每天早上提醒我看盘」
  它就直接建好并告诉你下次什么时候响
- 手里有现成模板：晨间简报 / 久坐提醒 / 喝水提醒 / 收盘复盘 / 睡前总结，一键加
- 任务列表显示「下次什么时候 / 还有多久」，可随时「跑一次 / 暂停 / 删除」
- 支持静默时段、任务日志（最近 60 条）

### 新增：定时任务会主动来找你

- 任务出结果后**主动送到眼前**：宠物气泡播报 + 系统通知（退化为托盘气泡）
  + 对话面板消息（面板没开会自动带出来），可选语音念一遍
- 结果会写进当前会话与历史，不会「弹完就没了」

### 新增：主动关注（地震 / 突发新闻 / 热点）

- 设置里新增「🔔 主动关注」页签，四类信源可单独开关：
  - **地震**：USGS 官方源，可设最小震级、范围（中国及周边 / 全球）；
    只报 45 分钟内的新震，首次运行只建基线不打扰
  - **热搜**：百度热搜、微博热搜（默认关），只报新上榜的条目
  - **关键词**：只推含关键词的条目（逗号分隔，留空推全部）
- 去重 24 小时，静默时段只记录不播报；「立即检查」可手动触发（无视静默时段）
- 所有播报都走同一套主动送达（气泡 / 通知 / 面板 / 可选语音）

### 测试

- 新增 `npm run test:proactive`（92 项）：时间解析与「下次何时」计算、
  静默时段跨零点、任务增删改查与真实执行、跑完一次的任务自动退休、
  没配模型时优雅失败、地震/热搜的基线-去重-阈值-地区-关键词-静默全路径
- `build/_proactive_smoke.js`（25 项，真 Electron + 真设置页）：
  宠物窗口尺寸自检与自动唤出、**高频移动 300 次不会把窗口撑大**、
  源码扫描禁止 `setPosition`、设置页新页签、定时任务端到端播报
- CI 的回归测试列表补上 `test:proactive`

---

## [1.5.1] - 2026-09-14

### 修复

- **所有对话请求默认走流式**。之前「测试连接」和子代理编排的规划/复核用的是非流式请求，
  遇到不支持非流式请求的服务商（返回 `HTTP 400 · 11101 Non-stream chat request is currently not supported`）
  会直接失败，首次配置时会卡在「测试连接」那一步，只能跳过。
  现在新增统一出口 `src/main/llm.js`：一律发 `stream: true`，再把分片拼成完整文本；
  同时兼容忽略 stream 参数、直接返回整包 JSON 的服务商，也兼容不写 `data:` 前缀的网关。
- 报错更具体：把服务端返回的 `msg` 带进错误提示，不再只剩一句 HTTP 400。

### 测试

- 新增 `npm run test:llm`（18 项）：源码扫描禁止出现 `stream: false`（防回归），
  并用本地假服务端跑通 SSE 拼接、整包 JSON 兼容、裸 JSON 行、400 错误、超时、缺 Key 等路径。

---

## [1.5.0] - 2026-09-14

### 新增：子代理编排（小问当主管，常任务自动分派）

- **自动分派**：遇到「多步骤 / 批量 / 要跑一会儿」的常任务，小问先拆成若干子任务派给子代理，
  派完立刻在对话里告诉主人派了哪些活，之后只做监督和收尾汇报，不再一步步请示
- **子代理会请示**：子代理执行中有 `ask_supervisor` 工具，拿不准就问小问；
  **小问优先决策**（不反手去问主人），直接给明确指令，子代理照做
- **小问监督**：每个子任务回来都要过小问一眼（复核目标是否达成），不过就改派重试，
  重试次数可在设置里配；一直失败也会如实汇报，不把主人卡死
- **任务看板**：对话窗口下方实时显示每个子任务的状态、摘要，以及「子代理问了什么 / 小问怎么决定的」
- **主动派活**：小问在普通对话里也能用 `delegate_task` 临时派一件差事给子代理
- 设置 → Agent 能力 → 子代理编排：开关、自动分派、是否复核、最大任务数 / 并发数 / 重试次数
- 回归测试 `npm run test:orch`（59 项：拆任务、请示决策、复核重试、中断、单任务派活）

### 新增：截图

- 快捷键 `Alt+Shift+A` 框选截图、`Alt+Shift+S` 整屏截图（都可在设置里改）
- 框选层：拖框、移动、四角手柄微调、双击截全屏、Esc 取消、Enter 完成，实时显示尺寸
- 截完自动存盘（`图片/小问截图`）+ 复制剪贴板 + 带到对话框，可以直接问小问「这报错啥意思」
- 模型侧新增 `screenshot` 工具：小问可以自己截屏看屏幕（失败自动退回系统命令）
- 设置 → 截图与更新：快捷键、截完之后的行为、保存目录

### 新增：自动更新

- 接入 electron-updater，从 GitHub Releases 拉取：启动 8 秒后静默检查，后台下载，
  下完弹一句「现在重启安装？」，不打断手上的事
- 设置 → 截图与更新 → 自动更新：开关、后台下载、退出时应用、预发布通道、手动检查 / 下载 / 安装
- 未安装更新组件或开发模式下全部降级为空操作，不影响使用

### 改进

- 宠物工具名汉化表补全为项目真实工具名（跑命令 / 读文件 / 看屏幕 / 请示小问 / 派活给子代理…）

### 打包发布

- **发版改为发编译产物**：新增 GitHub Actions（`.github/workflows/release.yml`），
  推 `vX.Y.Z` tag 就自动跑测试 → 编译 NSIS 安装包 → 把 exe / latest.yml / blockmap / 校验和
  一起挂到 Release 上，不再出现「只有源码、老用户更新不到」的发版
- **修掉产物名与更新清单不一致**：`artifactName` 固定为 `XiaoWen-Setup-${version}.${ext}`。
  以前中文产物名会被 electron-builder 在 `latest.yml` 里改写成拼音名，
  导致清单指向的资产不存在、自动更新 404
- 新增 `npm run release:upload`：本地编译后一键补挂 Release（找不到 Release 会按 CHANGELOG 自动创建）
- 新增 `npm run version:bump`：一次改好 `version` 与打包输出目录，避免两者对不上

---

## [1.4.0] — 2026-09-14 · 宠物 × Agent 联动

小问在后台干活时，桌上的宠物会同步演出——不用盯面板也知道它在干嘛。

### 新增 · 状态联动
- **思考中**：小问开始回答时，宠物头顶亮起「思考中」徽标，身体慢慢左右晃、眼睛往上瞟。
- **干活中**：调用工具时原地小跑，徽标变橙色，气泡里念出在做什么
  ——「正在读文件…」「正在跑命令…」「正在搜网页…」（内置工具已汉化，MCP 工具显示去掉前缀的名字）。
- **完成 / 失败**：答完蹦一下并加心情经验；出错会摇头、掉心情。
- **聆听中**：语音唤醒命中时宠物竖起耳朵「我在听…」。
- 睡觉中被叫起来干活会自己醒来；关掉联动会立刻收工。

### 健壮性
- 演出只做「表演」，全部包在 try/catch 里，宠物窗口没开或联动关闭时零开销、不影响对话。
- **兜底复位**：万一收尾事件丢了（请求中断、窗口卡过），60 秒后自动回待机，不会一直转圈；聆听态 15 秒。
- 新增 `npm run test:pet` 回归测试（32 项）：固化状态映射与工具名翻译，改坏一跑就知道。

### 其他
- 设置 → 🐧 桌面宠物 → **宠物 × Agent 联动**：可一键关掉。
- `config.example.json` 新增 `petAgentLink`。

---

## [1.3.0] — 2026-09-14 · 语音唤醒「小问小问」

### 新增 · 语音唤醒
- 说「小问」即可免按键唤起，自动打开对话面板并开始录音。
- **两级检测**：本地 VAD 常驻监听（只算音量，零网络请求）→ 检测到人声才开一次约 2~3 秒的短识别做关键词确认。
- **预缓冲 700ms**：识别会话建立前先把这段音频补发过去，唤醒词不会被截断。
- **同音字放宽**：`小问 / 小文 / 小闻 / 小吻` 等常见写法都能命中，唤醒词可在设置里自定义（逗号分隔）。
- **提示音**：命中时播放一声轻提示（可关）。
- 设置 → 语音 → 语音唤醒：开关、唤醒词、灵敏度（0~100）、提示音。
  未配置百炼 Key 时界面直接给出警示，避免「开了却唤不醒」。
- 悬浮球状态可视化：开启后核心一圈青色微光，识别中光环加速。

### 健壮性
- 面板打开 / 正在录音时自动让出麦克风，避免把 AI 的回答当成唤醒词反复触发。
  该状态与「临时暂停」分开记录，不会被定时器误恢复。
- **限流**：1 分钟内触发超过 12 次（开会、看电视等嘈杂场景）自动暂停 5 分钟并提示，避免烧配额。
- 识别服务无响应时有 4 秒兜底复位，不会卡在等待态。
- 窗口失焦导致音频时钟被 Chromium 挂起时自动 resume。

### 其他
- `config.example.json` 补充唤醒相关字段。
- README 新增「语音唤醒」章节，说明工作原理与取舍。

---

## [1.2.1] — 2026-09-14 · 安全止血 + 新机体验

### 安全
- **移除硬编码密钥**：`build/_jarvis_e2e.js` 里写死的阿里云百炼 Key 已删除，
  改为优先读环境变量 `DASHSCOPE_API_KEY`，其次读本机 `config.json`；未配置时该项自动跳过、不计失败。
- 新增 `.env.example` 模板，`.gitignore` 增加 `.env.example` 白名单，避免模板被误忽略。
- 用 `git filter-repo` 清理了 git 历史中的密钥痕迹（历史提交被重写，需重新 clone）。
- 补 `LICENSE`（MIT）—— `package.json` 早就声明 MIT，但文件一直缺失。
- ⚠️ 历史中曾出现过的百炼 Key 视为已泄露，**请到百炼控制台立即轮换/禁用**。

### 新增 · 首次启动配置引导
- 新机 clone 下来不再是一片空白：首次启动自动打开面板并弹出三步引导
  「欢迎 → 接入大模型（含连通性测试）→ 称呼 / 语音 Key（可跳过）」。
- 完成后写入 `config.setupDone`，之后不再打扰。
- 设置 → 常规 → **重新运行首次配置引导**，随时可以重走一遍（不清空已有配置）。
- 新增 `config.example.json` 配置模板。

### 文档
- README 同步：安装包路径改为 v1.2.0、补充首次引导说明、新增「密钥安全」章节、项目结构补充新文件。

---

## [1.2.0] — 2026-09-14 · 「桌面宠物」版本

像当年的 QQ 企鹅一样，桌面上多了一只可以养的小动物。

### 新增 · 桌面宠物
- **6 只手绘 SVG 动物可选**：🐧 企鹅 / 🐱 橘猫 / 🐼 熊猫 / 🐰 兔子 / 🐶 柴犬 / 🐸 青蛙，
  宠物右键菜单或设置页随时切换，每只都有专属台词库。
- **互动**：点击蹭蹭（开心跳 / 摇头 / 气泡搭话）、拖拽挪窝（自动切换朝向 + 贴地吸附）、
  双击唤起小问聊天、眼神跟随鼠标、随机眨眼与待机小动作。
- **养成**：心情 / 饱食 / 等级三围（悬停显示状态条），喂食 +饱食、互动 +心情 +经验，
  每 30 秒自然衰减，饿了会讨吃的，经验满升级。
- **行为**：睡觉（Zzz 动画）、自动散步（沿屏幕底部随机溜达，走路颠簸动画）、置顶开关。
- **播报**：小问回答结束后，宠物会用气泡复述一句摘要（可关）。
- **鼠标穿透**：窗口默认点击穿透，只有光标落在宠物身上才接管输入，完全不挡操作。

### 新增 · 设置与入口
- 设置页新增「🐧 桌面宠物」分组：动物选择卡片、大小（80~240px）、不透明度、
  置顶、自动散步、互动衰减开关、喂食 / 重置成长数据。
- 托盘菜单与悬浮球右键菜单新增「桌面宠物（开 / 关）」。

### 修复
- 眨眼与眼神跟随共用一个 `transform` 互相覆盖 —— 拆成 `.eye`（平移）/ `.eyeball`（缩放）分层。
- 散步首帧空目标导致原地等待 —— 现在开启散步立即出发。

---

## [1.1.0] — 2026-09-14 · 「贾维斯」版本

定位升级：从「聊天窗口」变成**能操作电脑的桌面智能体**。

### 新增 · Agent 能力
- **Agent 模式**：模型可自主调用工具完成多步操作（最多 8 轮工具循环），对话区实时展示
  工具调用卡片（执行中 / 成功 / 失败 / 已拒绝），高危操作弹出确认框。
- **17 个内置工具**：执行命令（PowerShell/cmd/bash）、读写删文件、列目录/搜索文件、
  打开应用或网址、进程列表与结束进程、**截屏**、剪贴板读写、系统通知、HTTP 请求、
  时间查询、系统信息、记忆读写。
- **安全护栏**：高危命令正则拦截（format / shutdown / reg / diskpart...）、
  写操作限制在用户目录/下载/临时目录/白名单、确认模式三档可调（仅高危 / 全部 / 自动）。

### 新增 · MCP 支持
- MCP 客户端（stdio + http 两种传输），JSON-RPC 2.0，`initialize / tools/list / tools/call`。
- 设置 → MCP 服务器：可视化添加/连接/断开/删除，启动时自动重连已启用的服务器。
- MCP 工具以 `mcp__<server>__<tool>` 形式并入 Agent 工具清单。

### 新增 · Skills 系统
- 兼容 `SKILL.md`（YAML frontmatter）约定，技能存于 `userData/jarvis/skills/`。
- 系统提示只注入技能清单，模型按需 `load_skill` 加载正文，避免撑爆上下文。
- 设置 → Skills：查看 / 新建 / 删除 / 打开技能目录；首次启动内置「每日简报」示例。

### 新增 · 记忆与人格
- **长期记忆**：`memory.json` 条目式存储，中文二字组 + 英文词混合检索打分
  （关键词命中 + 新鲜度衰减 + 热度），每次对话自动检索相关记忆注入上下文。
  模型可用 `memory_add` 工具主动记忆（听到"记住…"必存）。
- **短期记忆**：多会话管理（`sessions.json`），会话抽屉切换/新建/删除，
  旧版扁平历史自动迁移为会话。
- **人格系统**：助手名字/角色/性格/说话风格 + 主人称呼/别名/背景资料，
  全部拼装进 system prompt；界面问候语随人格变化。

### 新增 · 语音输出多方案
- **系统内置**（Web Speech，零配置离线）
- **阿里云百炼 CosyVoice**（cosyvoice-v1/v2，8 个实测可用音色，音质最佳，与识别共用 Key）
- **OpenAI 兼容 TTS**（`/v1/audio/speech`，自建或中转均可）
- 实测协议要点：CosyVoice 的 JSON 控制帧也以**二进制帧**下发（按首字节 `{` 区分），
  音频分片直接为 mp3/wav 二进制，服务端不主动发 `task-finished`，
  以「静默 900ms」判定合成结束。

### 新增 · 桌面集成与体验
- **开机自启**（Windows 注册表 Run / macOS LaunchAgent / Linux XDG autostart），设置页开关。
- 设置页重构为**分组导航**：常规 / AI 模型 / 人格与记忆 / Agent 能力 / MCP / Skills / 语音 / 显示与数据。
- 标题栏新增 **Agent 开关**（一键切换普通聊天与工具模式，不支持函数调用的模型自动降级）。
- 会话抽屉、消息操作条、toast 轻提示、快捷问题改为 Jarvis 风格示例。

### 验证
- 主进程端到端 22/22 通过：人格、记忆检索、PS/cmd 命令、高危拦截、文件读写删、
  越权写入拦截、进程列表、剪贴板、截屏（7MB PNG）、CosyVoice 合成（67KB MP3）、
  开机自启状态、Skills、MCP 连接/调用/工具清单。
- UI 冒烟：对话页与设置页 8 个分组加载零报错。

---

## [1.0.4] — 2026-09-14

### 修复
- **语音输入完全不可用（识别永远报 `network` 错误）**
  - 根因：原实现依赖 Chromium 内置的 `webkitSpeechRecognition`，该能力由 Google 云端服务提供，
    国内网络不可达，必然失败。这是技术选型问题，不是网络抖动。
  - 修复：改用阿里云百炼 paraformer-realtime（WebSocket 实时流式识别）。

### 新增
- 语音识别服务：阿里云百炼 paraformer-realtime，16kHz PCM 实时流式识别。
- 主进程 ASR 代理：`asr:start` / `asr:audio` / `asr:stop` / `asr:cancel` / `asr:test`。
- 设置 → 语音新增：识别方式选择、百炼 API Key、识别模型、静音自动结束时长、测试按钮。
- 静音自动发送：停止说话超过设定时长（默认 2 秒）自动提交，无需手动点击。
- 语音浮层波纹随实时音量起伏，中间结果与成品句子以光标区分。
- 系统内置识别保留为可切换的备选方案。

### 技术要点
- Electron 32 内置 Node 20.18，**没有全局 WebSocket**（该 API 到 Node 22 才默认可用）。
  主进程必须依赖 `ws` 包，且 `ws` 的消息回调签名 `(data, isBinary)` 与浏览器
  `(event)` 不同，需要兼容层处理。
- 识别音频为 16kHz 单声道 PCM，按 100ms 分片以二进制帧发送，不做 base64。

---

## [1.0.3] — 2026-09-13

### 修复
- **对话始终返回 HTTP 401 · Incorrect API key provided**
  - 根因：渲染进程把打码后的 Key（形如 `sk-abc***wxyz`）当作真实 Key 发给服务端。
  - 修复：所有大模型请求整体迁移到主进程，渲染进程只传消息内容，真实 Key 不再进入页面上下文。
  - 同步加固「保存」逻辑：输入框留空表示沿用旧 Key，含 `***` 的值一律拒绝落盘。

### 新增
- 主进程 API 代理：`chat:stream` / `chat:test` / `chat:abort`。
- 「停止生成」现在会真正中断 HTTP 请求，不再空耗 token。
- 网络类错误给出中文定位提示（DNS 失败 / 连接被拒 / 证书拦截 等）。

---

## [1.0.2] — 2026-09-13

### 修复
- **双击图标无反应（进程瞬间退出，exit code 0）**
  - 根因：上次被强杀后残留的 Chromium 单实例锁（`lockfile` / `SingletonLock`）导致
    `requestSingleInstanceLock()` 误判「已有实例在运行」，新进程静默 `app.quit()`。
  - 修复：启动时校验锁持有者 PID，仅在持有者已死时清理；再加一层独立信号
    （`tasklist` 进程探测）二次确认，最后重试取锁。
- **设置页面显示成了首页**
  - 根因：识别到设置模式后没有切换 DOM 显示状态。
  - 修复：显式 `showView()` 切换；主进程 `page:mode` 广播作为兜底。

### 新增
- 诊断日志 `~/.xiaowen/launch.log`（刻意不放在 userData —— Chromium 会清理该目录）。
- 启动看门狗：6 秒后自检悬浮球窗口是否创建且可见，异常则重建。
- `second-instance` 加固：确保已有实例的悬浮球可见并弹出面板。

---

## [1.0.1] — 2026-09-13

### 修复
- **部分机器双击后闪退（GPU 进程无法启动）**
  - 修复：Windows 默认走软件渲染；新增崩溃标记自动降级；保留 `--enable-gpu` 逃生门。

### 新增
- 设置项「禁用硬件加速」，附当前渲染模式与 GPU 合成状态展示。
- 设置项「立即重启程序」。

---

## [1.0.0] — 2026-09-12

### 新增
- 首个可用版本。
- 常驻桌面右下角的悬浮球，支持拖动与边缘吸附。
- 对话面板：流式输出、Markdown 渲染、代码块、复制 / 朗读 / 重答。
- 全局快捷键语音问答（默认 `Alt+Space`）。
- 对话历史本地持久化。
- 多服务商配置：DeepSeek / 通义千问 / Kimi / 智谱 GLM / OpenAI / 自定义。
- 系统托盘：打开面板、语音问答、设置、清空历史、退出。
